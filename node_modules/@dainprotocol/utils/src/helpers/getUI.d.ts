import { BaseUIElement } from "../generateUI";
/**
 * Extracts the UI elements from the DainResponse and returns
 * only those that have a renderMode of "inline" or "page".
 */
export declare function getUI(uiElements: BaseUIElement<any>[], renderMode: "inline" | "page"): BaseUIElement<any>[];
