"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUI = getUI;
/**
 * Extracts the UI elements from the DainResponse and returns
 * only those that have a renderMode of "inline" or "page".
 */
function getUI(uiElements, renderMode) {
    if (!uiElements) {
        return [];
    }
    const uiArray = (Array.isArray(uiElements)
        ? uiElements
        : [uiElements]);
    return uiArray.filter((uiElement) => uiElement.renderMode === renderMode);
}
//# sourceMappingURL=getUI.js.map