import { DSLBuilder } from "../../../base/base.builder";
import { DslAction } from "../../../types";
export interface OrderQuote {
    total_with_tip: number;
    tip: number;
    store_address: string;
    store: string;
    store_id: string;
    quote_id: string;
    quote: unknown;
    expected_time_of_arrival: string;
}
export interface OrderSuccessProps {
    orderId: string;
    trackingLink: string;
    isSandbox: boolean;
    finalQuote: OrderQuote;
}
export declare class OrderSuccessUIBuilder extends DSLBuilder<OrderSuccessProps> {
    constructor(orderId: string, trackingLink: string, finalQuote: OrderQuote, initialProps?: Partial<OrderSuccessProps>, renderMode?: "inline" | "page");
    /**
     * Sets whether this is a sandbox/test order
     */
    setSandboxMode(isSandbox: boolean): this;
    /**
     * Updates the tracking link for the order
     */
    setTrackingLink(trackingLink: string): this;
    /**
     * Updates the final quote details
     */
    setFinalQuote(quote: OrderQuote): this;
    /**
     * Defines the action to be triggered when tracking the order
     */
    onTrackOrder(action: Exclude<Partial<DslAction>, "type">): this;
    /**
     * Defines the action to be triggered when viewing order details
     * TODO: Implement this in FE
     */
    onViewDetails(action: Exclude<Partial<DslAction>, "type">): this;
}
