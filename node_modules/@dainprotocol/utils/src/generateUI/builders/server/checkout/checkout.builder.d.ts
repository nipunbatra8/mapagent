import { DSLBuilder } from "../../../base/base.builder";
import { DslAction } from "../../../types";
export interface Address {
    street_num: string;
    street_name: string;
    city: string;
    state: string;
    country: string;
    zipcode: string;
    apt_num: string;
}
export interface DeliveryDetails {
    name: string;
    email: string;
    phone: string;
    address: Address;
    dropoffNotes: string;
}
export interface CheckoutProps {
    orderId: string;
    quoteId: string;
    totalCost: number;
    store: string;
    storeId: string;
    deliveryDetails: DeliveryDetails;
}
/**
 * A DSL builder class that creates a "checkout" UI element,
 * with order details, delivery information, and payment actions.
 */
export declare class CheckoutUIBuilder extends DSLBuilder<CheckoutProps> {
    constructor(orderId: string, quoteId: string, initialProps?: Partial<CheckoutProps>, renderMode?: "inline" | "page");
    /**
     * Sets the total cost of the order
     */
    setTotalCost(cost: number): this;
    /**
     * Sets the store information
     */
    setStore(store: string, storeId: string): this;
    /**
     * Sets the delivery details
     */
    setDeliveryDetails(details: DeliveryDetails): this;
    /**
     * Defines the action to be triggered when proceeding to payment
     */
    onProceedToPayment(action: Exclude<Partial<DslAction>, "type">): this;
    /**
     * Defines the action to be triggered when canceling the order
     */
    onCancelOrder(action: Exclude<Partial<DslAction>, "type">): this;
}
