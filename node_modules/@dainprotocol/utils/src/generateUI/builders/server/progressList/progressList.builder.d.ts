import { DSLBuilder } from "../../../base/base.builder";
import { DslAction } from "../../../types";
export interface ProgressItem {
    label: string;
    value: number;
    max?: number;
    color?: string;
    description?: string;
}
export interface ProgressListProps {
    title?: string;
    items: ProgressItem[];
}
export declare class ProgressListUIBuilder extends DSLBuilder<ProgressListProps> {
    constructor(initialProps?: Partial<ProgressListProps>, renderMode?: "inline" | "page");
    /**
     * Sets the title for the progress list
     */
    title(title: string): this;
    /**
     * Adds a single progress item to the list
     */
    addItem(item: ProgressItem): this;
    /**
     * Adds multiple progress items at once
     */
    addItems(items: ProgressItem[]): this;
    /**
     * Defines the action to be triggered when an item is selected
     */
    onSelectItem(action: Exclude<Partial<DslAction>, "type">): this;
}
