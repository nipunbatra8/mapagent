import { DSLBuilder } from "../../../base/base.builder";
import { DslAction } from "../../../types";
export interface MenuItem {
    productId: string;
    name: string;
    image: string;
    description: string;
    price: number;
    requiresCustomization: boolean;
}
export interface MenuCategory {
    name: string;
    products: MenuItem[];
}
export interface MenuProps {
    categories: MenuCategory[];
    totalPages: number;
    currentPage: number;
}
export declare class MenuUIBuilder extends DSLBuilder<MenuProps> {
    constructor(initialProps?: Partial<MenuProps>, renderMode?: "inline" | "page");
    /**
     * Sets pagination details
     */
    setPagination(currentPage: number, totalPages: number): this;
    /**
     * Adds a single category with its products
     */
    addCategory(category: MenuCategory): this;
    /**
     * Adds multiple categories at once
     */
    addCategories(categories: MenuCategory[]): this;
    /**
     * Defines the action to be triggered when adding a product to order
     */
    onAddToOrder(action: Exclude<Partial<DslAction>, "type">): this;
    /**
     * Defines the action to be triggered when changing categories
     */
    onCategoryChange(action: Exclude<Partial<DslAction>, "type">): this;
}
