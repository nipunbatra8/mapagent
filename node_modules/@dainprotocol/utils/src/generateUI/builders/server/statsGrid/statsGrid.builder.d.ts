import { DSLBuilder } from "../../../base/base.builder";
import { DslAction } from "../../../types";
export interface StatItem {
    title: string;
    value: string | number;
    change?: number;
    description?: string;
    prefix?: string;
    suffix?: string;
}
export interface StatsGridProps {
    title?: string;
    stats: StatItem[];
    baseParams?: Record<string, unknown>;
}
/**
 * A DSL builder class that creates a "statsGrid" UI element,
 * with dynamic stats and optional refresh action.
 */
export declare class StatsGridUIBuilder extends DSLBuilder<StatsGridProps> {
    constructor(initialProps?: Partial<StatsGridProps>, renderMode?: "inline" | "page");
    /**
     * Sets the title for the stats grid
     */
    title(title: string): this;
    /**
     * Sets base parameters that will be used for all stats
     */
    setBaseParams(params: Record<string, unknown>): this;
    /**
     * Adds a single stat to the grid
     */
    addStat(stat: StatItem): this;
    /**
     * Adds multiple stats at once
     */
    addStats(stats: StatItem[]): this;
    onItemClick(action: Exclude<Partial<DslAction>, "type">): this;
}
