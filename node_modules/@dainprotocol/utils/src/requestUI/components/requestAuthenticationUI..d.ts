import { DainResponse } from '../../DainResponse';
interface AppWithAuthUrl {
    oauth2?: {
        generateAuthUrl: (provider: string, agentId: string) => Promise<string>;
    };
}
export type AgentInfo = {
    agentId: string;
    address: string;
    smartAccountPDA?: string;
    id: string;
    webhookUrl?: string;
};
interface RequestAuthentication {
    app: AppWithAuthUrl;
    agentInfo: AgentInfo;
}
export declare const requestAuthenticationTool: ({ provider, logo }: {
    provider: string;
    logo: string;
}, { app, agentInfo }: RequestAuthentication) => Promise<DainResponse>;
export {};
